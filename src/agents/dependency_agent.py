class DependencyAgent:
    """
    Builds dependency graph (which step depends on which).
    """

    def run(self, steps: list) -> dict:
        # TODO: Implement Gemini reasoning
        return {
            "graph": {},
            "parallel_steps": [],
            "critical_path": [],
        }
